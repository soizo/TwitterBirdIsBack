{
  "name": "twitter-bird-is-back",
  "version": "0.1.3",
  "private": true,
  "description": "Restore the classic Twitter bird, terminology, and blue buttons in Chrome and Firefox.",
  "scripts": {
    "test": "node --test",
    "package": "npm run build && python3 scripts/package-extension.py",
    "build": "node -e \"require('node:fs').cpSync('extension', 'dist', { recursive: true })\""
  },
  "devDependencies": {
    "playwright": "^1.63.0"
  }
}
