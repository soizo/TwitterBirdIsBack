# 本项目工作约定

- 每完成一个范围明确的小任务，自动创建对应的本地 Git 提交，不必再次询问。代码任务须先通过相关测试并构建 `dist/`。
- 提交前检查差异，只纳入当前任务的源文件、测试和文档；不混入无关改动或构建产物、缓存、运行时数据。
- 不推送；远端操作仍须另行明确授权。
- 用户要求更新／发布插件到 Mozilla Add-ons 时，先读取并复用 `skills/update-addon/SKILL.md`，默认附上源码及可复现打包说明，核实远端保存结果；发布和 GitHub 推送仍分别需要当次授权。
- 扩展自身面向用户的名称、描述、提示和各语言文案统一使用 Twitter，不提改名后的品牌。内部域名、选择器及官方来源记录保留真实值。
- 激光眼蓝鸟仅用于扩展工具栏、管理页和弹窗；网页启动屏、站内品牌图标及网站图标仍用经典蓝鸟。
